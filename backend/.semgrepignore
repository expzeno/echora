# Suppress Semgrep false positives from Express handler patterns.
# Semgrep's taint rules flag req/res variable flow in Express handlers even
# when values are validated — these are not real injection risks.
src/modules/
src/shared/
